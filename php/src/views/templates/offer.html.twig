{% extends "base.html.twig" %}

{% block title %}{{ offer.name }} - Интернет-магазин{% endblock %}

{% block content %}
<!-- Breadcrumb -->
<nav aria-label="breadcrumb" class="mb-4">
    <ol class="breadcrumb">
        <li class="breadcrumb-item"><a href="/">Главная</a></li>
        {% if section %}
            <li class="breadcrumb-item"><a href="/section?id={{ section.id }}">{{ section.name }}</a></li>
        {% endif %}
        <li class="breadcrumb-item active" aria-current="page">{{ offer.name }}</li>
    </ol>
</nav>

<!-- Product Details -->
<div class="row mb-5">
    <!-- Product Images -->
    <div class="col-md-6 mb-4">
        {% if offer.images is defined and offer.images %}
            <div id="productCarousel" class="carousel slide" data-bs-ride="carousel">
                <div class="carousel-inner">
                    {% for image in offer.images %}
                        <div class="carousel-item {% if loop.first %}active{% endif %}">
                            <img src="{{ image }}" class="d-block w-100 rounded" alt="{{ offer.name }}" style="height: 400px; object-fit: cover;">
                        </div>
                    {% endfor %}
                </div>
                {% if offer.images|length > 1 %}
                    <button class="carousel-control-prev" type="button" data-bs-target="#productCarousel" data-bs-slide="prev">
                        <span class="carousel-control-prev-icon"></span>
                    </button>
                    <button class="carousel-control-next" type="button" data-bs-target="#productCarousel" data-bs-slide="next">
                        <span class="carousel-control-next-icon"></span>
                    </button>
                {% endif %}
            </div>
        {% elseif offer.image %}
            <img src="{{ offer.image }}" class="img-fluid rounded" alt="{{ offer.name }}" style="width: 100%; height: 400px; object-fit: cover;">
        {% else %}
            <div class="bg-light rounded d-flex align-items-center justify-content-center" style="height: 400px;">
                <i class="bi bi-image text-muted" style="font-size: 5rem;"></i>
            </div>
        {% endif %}
    </div>
    
    <!-- Product Info -->
    <div class="col-md-6">
        <h1 class="mb-3">{{ offer.name }}</h1>
        
        <!-- Price -->
        <div class="mb-4">
            <div class="d-flex align-items-center">
                <span class="h2 text-primary me-3">{{ offer.price }} ₽</span>
                {% if offer.old_price %}
                    <span class="h5 text-muted text-decoration-line-through">{{ offer.old_price }} ₽</span>
                    <span class="badge bg-danger ms-2">
                        -{{ ((offer.old_price - offer.price) / offer.old_price * 100)|round }}%
                    </span>
                {% endif %}
            </div>
        </div>
        
        <!-- Availability -->
        <div class="mb-4">
            {% if offer.quantity is defined and offer.quantity > 0 %}
                <span class="badge bg-success fs-6">
                    <i class="bi bi-check-circle"></i>
                    В наличии ({{ offer.quantity }} шт.)
                </span>
            {% else %}
                <span class="badge bg-warning fs-6">
                    <i class="bi bi-clock"></i>
                    Под заказ
                </span>
            {% endif %}
        </div>
        
        <!-- Description -->
        {% if offer.description %}
            <div class="mb-4">
                <h5>Описание</h5>
                <p class="text-muted">{{ offer.description }}</p>
            </div>
        {% endif %}
        
        <!-- Properties -->
        {% if offer.properties is defined and offer.properties %}
            <div class="mb-4">
                <h5>Характеристики</h5>
                <div class="table-responsive">
                    <table class="table table-sm">
                        {% for property in offer.properties %}
                            <tr>
                                <td class="fw-bold" style="width: 40%;">{{ property.name }}:</td>
                                <td>{{ property.value }}</td>
                            </tr>
                        {% endfor %}
                    </table>
                </div>
            </div>
        {% endif %}
        
        <!-- Add to Cart -->
        {% if user %}
            <div class="mb-4">
                <div class="row g-3">
                    <div class="col-auto">
                        <label for="quantity" class="form-label">Количество:</label>
                        <input type="number" class="form-control" id="quantity" value="1" min="1" max="{{ offer.quantity|default(99) }}" style="width: 80px;">
                    </div>
                    <div class="col-auto d-flex align-items-end">
                        <button class="btn btn-primary btn-lg" id="addToCartBtn" data-offer-id="{{ offer.id }}">
                            <i class="bi bi-cart-plus"></i>
                            Добавить в корзину
                        </button>
                    </div>
                </div>
            </div>
        {% else %}
            <div class="alert alert-info">
                <i class="bi bi-info-circle"></i>
                <a href="/login" class="alert-link">Войдите в систему</a>, чтобы добавить товар в корзину
            </div>
        {% endif %}
        
        <!-- Share -->
        <div class="mb-4">
            <h6>Поделиться:</h6>
            <div class="btn-group" role="group">
                <button type="button" class="btn btn-outline-secondary btn-sm" onclick="shareProduct('vk')">
                    <i class="bi bi-share"></i> VK
                </button>
                <button type="button" class="btn btn-outline-secondary btn-sm" onclick="shareProduct('telegram')">
                    <i class="bi bi-telegram"></i> Telegram
                </button>
                <button type="button" class="btn btn-outline-secondary btn-sm" onclick="copyLink()">
                    <i class="bi bi-link-45deg"></i> Копировать ссылку
                </button>
            </div>
        </div>
    </div>
</div>

<!-- Related Offers -->
{% if related_offers is defined and related_offers %}
<div class="row mb-5">
    <div class="col-12">
        <h3 class="mb-4">Другие варианты этого товара</h3>
    </div>
    {% for related_offer in related_offers %}
        <div class="col-md-6 col-lg-3 mb-4">
            <div class="card h-100 border-0 shadow-sm product-card">
                {% if related_offer.image %}
                    <img src="{{ related_offer.image }}" class="card-img-top" alt="{{ related_offer.name }}" style="height: 200px; object-fit: cover;">
                {% else %}
                    <div class="card-img-top bg-light d-flex align-items-center justify-content-center" style="height: 200px;">
                        <i class="bi bi-image text-muted" style="font-size: 2rem;"></i>
                    </div>
                {% endif %}
                
                <div class="card-body d-flex flex-column">
                    <h6 class="card-title">
                        <a href="/offer?id={{ related_offer.id }}" class="text-decoration-none">{{ related_offer.name }}</a>
                    </h6>
                    
                    <div class="mt-auto">
                        <div class="d-flex justify-content-between align-items-center mb-2">
                            <span class="h6 text-primary mb-0">{{ related_offer.price }} ₽</span>
                        </div>
                        
                        <div class="d-grid">
                            <a href="/offer?id={{ related_offer.id }}" class="btn btn-outline-primary btn-sm">
                                Посмотреть
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    {% endfor %}
</div>
{% endif %}

<!-- Similar Products -->
{% if similar_offers is defined and similar_offers %}
<div class="row">
    <div class="col-12">
        <h3 class="mb-4">Похожие товары</h3>
    </div>
    {% for similar_offer in similar_offers %}
        <div class="col-md-6 col-lg-3 mb-4">
            <div class="card h-100 border-0 shadow-sm product-card">
                {% if similar_offer.image %}
                    <img src="{{ similar_offer.image }}" class="card-img-top" alt="{{ similar_offer.name }}" style="height: 200px; object-fit: cover;">
                {% else %}
                    <div class="card-img-top bg-light d-flex align-items-center justify-content-center" style="height: 200px;">
                        <i class="bi bi-image text-muted" style="font-size: 2rem;"></i>
                    </div>
                {% endif %}
                
                <div class="card-body d-flex flex-column">
                    <h6 class="card-title">
                        <a href="/offer?id={{ similar_offer.id }}" class="text-decoration-none">{{ similar_offer.name }}</a>
                    </h6>
                    
                    <div class="mt-auto">
                        <div class="d-flex justify-content-between align-items-center mb-2">
                            <span class="h6 text-primary mb-0">{{ similar_offer.price }} ₽</span>
                        </div>
                        
                        <div class="d-grid gap-2">
                            <a href="/offer?id={{ similar_offer.id }}" class="btn btn-outline-primary btn-sm">
                                Посмотреть
                            </a>
                            {% if user %}
                                <button class="btn btn-primary btn-sm add-to-cart" data-offer-id="{{ similar_offer.id }}">
                                    <i class="bi bi-cart-plus"></i>
                                    В корзину
                                </button>
                            {% endif %}
                        </div>
                    </div>
                </div>
            </div>
        </div>
    {% endfor %}
</div>
{% endif %}
{% endblock %}

{% block javascripts %}
<script>
document.addEventListener('DOMContentLoaded', function() {
    // Main add to cart button
    const addToCartBtn = document.getElementById('addToCartBtn');
    const quantityInput = document.getElementById('quantity');
    
    if (addToCartBtn) {
        addToCartBtn.addEventListener('click', function() {
            const offerId = this.dataset.offerId;
            const quantity = parseInt(quantityInput.value) || 1;
            const originalText = this.innerHTML;
            
            // Disable button and show loading
            this.disabled = true;
            this.innerHTML = '<i class="bi bi-hourglass-split"></i> Добавление...';
            
            fetch('/api/cart', {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json',
                },
                body: JSON.stringify({
                    offer_id: offerId,
                    quantity: quantity
                })
            })
            .then(response => response.json())
            .then(data => {
                if (data.message) {
                    this.innerHTML = '<i class="bi bi-check"></i> Добавлено в корзину';
                    this.classList.remove('btn-primary');
                    this.classList.add('btn-success');
                    
                    // Update cart counter if exists
                    const cartBadge = document.querySelector('.cart-badge');
                    if (cartBadge) {
                        const currentCount = parseInt(cartBadge.textContent) || 0;
                        cartBadge.textContent = currentCount + quantity;
                    }
                    
                    setTimeout(() => {
                        this.innerHTML = originalText;
                        this.classList.remove('btn-success');
                        this.classList.add('btn-primary');
                        this.disabled = false;
                    }, 3000);
                } else {
                    throw new Error(data.error || data.message || 'Ошибка добавления в корзину');
                }
            })
            .catch(error => {
                console.error('Error:', error);
                this.innerHTML = '<i class="bi bi-exclamation-triangle"></i> Ошибка';
                this.classList.remove('btn-primary');
                this.classList.add('btn-danger');
                
                setTimeout(() => {
                    this.innerHTML = originalText;
                    this.classList.remove('btn-danger');
                    this.classList.add('btn-primary');
                    this.disabled = false;
                }, 3000);
            });
        });
    }
    
    // Similar products add to cart buttons
    const addToCartButtons = document.querySelectorAll('.add-to-cart');
    
    addToCartButtons.forEach(button => {
        button.addEventListener('click', function() {
            const offerId = this.dataset.offerId;
            const originalText = this.innerHTML;
            
            // Disable button and show loading
            this.disabled = true;
            this.innerHTML = '<i class="bi bi-hourglass-split"></i> Добавление...';
            
            fetch('/api/cart', {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json',
                },
                body: JSON.stringify({
                    offer_id: offerId,
                    quantity: 1
                })
            })
            .then(response => response.json())
            .then(data => {
                if (data.message) {
                    this.innerHTML = '<i class="bi bi-check"></i> Добавлено';
                    this.classList.remove('btn-primary');
                    this.classList.add('btn-success');
                    
                    // Update cart counter if exists
                    const cartBadge = document.querySelector('.cart-badge');
                    if (cartBadge) {
                        const currentCount = parseInt(cartBadge.textContent) || 0;
                        cartBadge.textContent = currentCount + 1;
                    }
                    
                    setTimeout(() => {
                        this.innerHTML = originalText;
                        this.classList.remove('btn-success');
                        this.classList.add('btn-primary');
                        this.disabled = false;
                    }, 2000);
                } else {
                    throw new Error(data.error || data.message || 'Ошибка добавления в корзину');
                }
            })
            .catch(error => {
                console.error('Error:', error);
                this.innerHTML = '<i class="bi bi-exclamation-triangle"></i> Ошибка';
                this.classList.remove('btn-primary');
                this.classList.add('btn-danger');
                
                setTimeout(() => {
                    this.innerHTML = originalText;
                    this.classList.remove('btn-danger');
                    this.classList.add('btn-primary');
                    this.disabled = false;
                }, 2000);
            });
        });
    });
});

// Share functions
function shareProduct(platform) {
    const url = encodeURIComponent(window.location.href);
    const title = encodeURIComponent(document.title);
    
    let shareUrl;
    switch(platform) {
        case 'vk':
            shareUrl = `https://vk.com/share.php?url=${url}&title=${title}`;
            break;
        case 'telegram':
            shareUrl = `https://t.me/share/url?url=${url}&text=${title}`;
            break;
    }
    
    if (shareUrl) {
        window.open(shareUrl, '_blank', 'width=600,height=400');
    }
}

function copyLink() {
    navigator.clipboard.writeText(window.location.href).then(function() {
        // Show success message
        const btn = event.target.closest('button');
        const originalText = btn.innerHTML;
        btn.innerHTML = '<i class="bi bi-check"></i> Скопировано';
        btn.classList.add('btn-success');
        btn.classList.remove('btn-outline-secondary');
        
        setTimeout(() => {
            btn.innerHTML = originalText;
            btn.classList.remove('btn-success');
            btn.classList.add('btn-outline-secondary');
        }, 2000);
    });
}
</script>
{% endblock %}

